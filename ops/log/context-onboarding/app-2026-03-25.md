# Onboarding Log — app (Main Website)

**Date:** 2026-03-25
**Preset:** repository
**Status after onboarding:** needs_setup

---

## Summary

Updated the existing `app` context source and `edit-app` action to reflect
that this is the **main website application** being actively developed and that
agents should be working on it.

---

## Changes Made

### context/app/source.yml
- Changed `type`: `folder` → `repository`
- Changed `status`: `active` → `needs_setup`
- Updated `name` to clarify this is the "Main Website"
- Added `remote_url: ""` placeholder (no canonical remote provided)
- Replaced `origin_path` with `provisional_local_path`
- Updated `description` and `notes`
- Updated `last_updated` to 2026-03-25

### context/app/README.md
- Updated header to show `needs_setup` status
- Added callout box explaining needs_setup and that edit-app is still active
- Updated "What It Is" to frame this as the primary app being built
- Updated "How to Run" paths to use absolute machine path

### context/app/requirements.md
- Changed status from "complete" to `needs_setup`
- Documented the missing canonical git remote URL as the outstanding blocker
- Added step-by-step instructions for completing setup

### context/registry.yml
- Added new entry for `app` source (type: repository, status: needs_setup)
  at the top of the context list

### actions/edit-app/action.yml
- Changed `status`: `inactive` → `active`
- Updated `name` to include "(Main Website)"
- Changed target `kind`: `filesystem` → `repository`
- Added `remote_url: ""` placeholder
- Expanded `instructions` with project conventions and git workflow guidance

### actions/edit-app/README.md
- Updated status to `active`
- Added clear instructions for agents
- Added note about provisional path vs. canonical remote
- Added link to requirements.md for completing setup

### actions/registry.yml
- Updated name and status for `edit-app` entry

---

## What Agents Can Do Now

- **Read:** Full source context available at `context/app/` and snapshot at
  `context/app/repo/` (if cloned)
- **Write:** Agents may edit files at `/Users/seima/Documents/cursor/agent-os/app`
  via the `edit-app` action (now active)

---

## Outstanding Requirements

1. **Canonical git remote URL** — must be added to `source.yml` and
   `action.yml` to promote status to `active`

---

## Decisions & Assumptions

- No canonical git remote URL was provided (only the relative local path
  `../app`), so status was set to `needs_setup` per onboarding rules.
- The `edit-app` action was promoted to `active` because the user explicitly
  stated "this is what we'll be working on" and the local writable path is
  clearly known.
- The provisional local path was resolved to the absolute machine path
  `/Users/seima/Documents/cursor/agent-os/app` (parent of ops/).
- The local `context/app/repo/` snapshot was not re-cloned (no remote URL
  available to clone from).
